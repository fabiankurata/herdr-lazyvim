# Server namespaces

`server_namespaces.py` presents observed synthetic keys from two separately owned named servers. Overlapping local IDs are valid only when session and socket authority differ. Local tests use a fake CLI and do not provide native evidence. Queue-delayed effects remain UNVERIFIED in this lane.
